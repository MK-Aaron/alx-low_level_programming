#ifndef HEADER_FILE
#define HEADER_FILE

/* C-Libraries */
#include <stdio.h>
#include <stdlib.h>

/* Prototypes */
unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);

#endif

