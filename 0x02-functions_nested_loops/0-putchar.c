#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 (success)
 *
 */

int main(void)
{
	char hello[9] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r', '\0'};

	printf("%s\n", hello);

	return (0);
}
